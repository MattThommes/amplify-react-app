version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm install v18.13.0
        - npm install -g npm@9
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
